[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-testing-research"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["pytest", "pandas"]

[tool.setuptools]
packages = ["python_testing_research"]