def test_is_price_data_valid():
    # Please fill in :)
    # Maybe test a positive and negative case with pytest.mark.parametrize
    assert True

def test_market_simulates_without_failing():
    # Interact with the market model in a loop with `step()` and see if it runs without throwing an exception
    assert True

def test_market_handles_trading_correctly():
    # Loop though time (use `step()`); Send trades to the market: Check if it clears and if the trade data is stored in
    # the market
    assert True